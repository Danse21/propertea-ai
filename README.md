## Git flow

We want to keep clean naming. So we will use specific prefixes for our branches:
`feat` - for new features on dev branch
`fix` - for fixing bugs and issues on dev branch
`hotfix` - for fixes on main
